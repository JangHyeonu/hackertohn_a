import 'package:flutter/material.dart';

class CustomTextFormComponent extends StatelessWidget {
  const CustomTextFormComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return TextFormField(

    );
  }
}
